import * as React from 'react';
import { Text, TouchableOpacity, View } from 'react-native';
import styles from '../../styles';


export const AnOtherTab = () => {

    return (
        <View style={styles.screenView}>

            <Text style={styles.screenText}>Other tab</Text>

        </View>
    );
}